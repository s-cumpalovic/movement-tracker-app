import React from 'react';

const NoRecords: React.FC = () => (
  <div className="no-records-container">
    <h1 className="no-records-headline">You currently have no records.</h1>
  </div>
);

export default NoRecords;
